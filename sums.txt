private int tinhTong(int n) {
    int sum = 0;

    for (int i = 0; i < n; i++) {
        int number;
        std::cout << "Nhap so thu " << (i + 1) << ": ";
        std::cin >> number;
        sum += number;
    }

    return sum;
}